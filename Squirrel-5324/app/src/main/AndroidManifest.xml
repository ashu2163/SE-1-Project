<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myApp.simpleregistrationandloginapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".Login"
            android:configChanges="orientation"
            android:screenOrientation="portrait"
            android:theme="@style/NoTitle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ManagerHomeActivity" android:theme="@style/NoTitle" />
        <activity android:name=".UserHomeActivity" android:theme="@style/NoTitle" />
        <activity android:name=".OperatorHomeActivity" android:theme="@style/NoTitle" />

        <activity android:name=".ConfirmEmailActivity"
                  android:theme="@style/NoTitle"/>

        <activity android:name=".ForgotPasswordActivity"
                  android:theme="@style/NoTitle"/>

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation"
            android:label="@string/app_name"
            android:theme="@style/NoTitle">
            <intent-filter>
                <action android:name="com.razormist.simpleregistrationandloginapplication.Login" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>